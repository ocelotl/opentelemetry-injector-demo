[project]
name = "opentelemetry-injector-demo"
version = "0.0.0"
requires-python = ">=3.11"

[dependency-groups]
dev = ["pytest>=7", "pytest-benchmark>=4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["tests"]
